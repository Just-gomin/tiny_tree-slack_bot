# ================================
# Slack 설정
# ================================
# Slack App-Level Token (Socket Mode 연결에 사용, xapp-로 시작)
SLACK_APP_TOKEN=xapp-your-token-here

# Slack Bot User OAuth Token (API 호출용, xoxb-로 시작)
SLACK_BOT_TOKEN=xoxb-your-token-here

# Slack Signing Secret (요청 검증용)
SLACK_SIGNING_SECRET=your-signing-secret-here

# ================================
# Claude Code 설정
# ================================
# Anthropic API Key for Claude
ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# Claude Code CLI 실행 파일 경로 (사전에 `claude login` 필요)
CLAUDE_CODE_PATH=/usr/local/bin/claude

# ================================
# 프로젝트 경로 설정
# ================================
# Tiny Tree Flutter 프로젝트 루트 경로
TINY_TREE_PATH=/Users/kangmin/projects/tiny_tree

# ================================
# Firebase 설정
# ================================
# Firebase 프로젝트 ID (Preview Channel 배포에 사용)
FIREBASE_PROJECT_ID=your-firebase-project-id

# Firebase CI Token (generate with: firebase login:ci)
# 최소 권한 원칙 적용 권장
FIREBASE_TOKEN=your-firebase-ci-token

# ================================
# 애플리케이션 설정
# ================================
# NestJS 서버 포트 (기본값: 3000)
PORT=3000

# 실행 환경 (development, production, test)
NODE_ENV=development

# Node.js 메모리 설정 (GCP e2-micro는 1GB RAM)
# OOM 방지를 위해 768MB로 제한 권장
# NODE_OPTIONS=--max-old-space-size=768
